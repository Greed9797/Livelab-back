# test/disponibilidade.test.js

- buildApp · function · L9-L21 — function buildApp({ papel = 'franqueado', queryImpl } = {})
- makeQueryImpl · function · L24-L35 — function makeQueryImpl({ apresentadora = { id: APR_ID, user_id: 'apr-user' }, dow = 1, slots = [{}], bloqueios = [], lives = [] } = {})
