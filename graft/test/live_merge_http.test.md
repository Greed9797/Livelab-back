# test/live_merge_http.test.js

- constructor · method · L24-L29 — constructor(message, { code, statusCode = 400, blockers } = {})
- mergePayload · function · L53-L62 — function mergePayload(overrides = {})
- buildApp · function · L64-L84 — async function buildApp({ papel = 'gerente', audit = true } = {})
